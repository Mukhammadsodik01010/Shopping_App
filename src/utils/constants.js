class CollectionNames {
    static USER = "user"
    static SAVE_FILE = "save_file"
}

class RoleNames {
    static ADMIN = "admin"
    static USER = "user"
}

module.exports = {CollectionNames, RoleNames}